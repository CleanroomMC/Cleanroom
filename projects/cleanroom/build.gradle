plugins {
    id 'java'
}

group 'com.cleanroommc'
version cleanroom_version

tasks.register('setup') {
    group 'setup'
    dependsOn project(':forge').tasks.named('generateForgeDiffs')

    doLast {
        println 'Setting up workspace for Cleanroom...'
    }
}