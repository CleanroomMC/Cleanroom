
def props = project.properties

apply plugin: 'top.outlands.gradle.mcp'

repositories {
    maven {
        name = "outlandsReleases"
        url = "https://maven.outlands.top/releases"
    }
}

mcp {
    config = props.minecraft_version + '-' + props.mcp_version
    pipeline = 'joined'
}