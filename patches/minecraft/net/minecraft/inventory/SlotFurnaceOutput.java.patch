--- before/net/minecraft/inventory/SlotFurnaceOutput.java
+++ after/net/minecraft/inventory/SlotFurnaceOutput.java
@@ -57,7 +57,10 @@
         if (!this.player.world.isRemote)
         {
             int i = this.removeCount;
-            float f = FurnaceRecipes.instance().getSmeltingExperience(stack);
+            float f = 0.0F;
+            if (this.inventory instanceof com.cleanroommc.common.crafting.IFurnace) {
+                f = ((com.cleanroommc.common.crafting.IFurnace) this.inventory).extractExp();
+            }
 
             if (f == 0.0F)
             {
@@ -94,5 +97,6 @@
         }
 
         this.removeCount = 0;
+        net.minecraftforge.fml.common.FMLCommonHandler.instance().firePlayerSmeltedEvent(player, stack);
     }
 }
